# Database Configuration
# For macOS (Homebrew PostgreSQL):
DATABASE_URL=postgresql://YOUR_USERNAME@localhost:5432/url_shortener
# For Linux/Windows with password:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/url_shortener

# Server Configuration
PORT=3000
BASE_URL=http://localhost:3000

# JWT Secret (ВАЖНО: Измените на свой уникальный секрет!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345678

# Email Configuration (опционально, для production)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
