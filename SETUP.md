# 🚀 Инструкция по настройке URL Shortener

## 📋 Требования

- Node.js 16+
- PostgreSQL 12+
- npm или yarn

## 🔧 Пошаговая настройка

### 1. Установка PostgreSQL

#### Windows:
1. Скачайте PostgreSQL с официального сайта: https://www.postgresql.org/download/windows/
2. Установите PostgreSQL (запомните пароль для пользователя `postgres`)
3. Добавьте PostgreSQL в PATH (обычно `C:\Program Files\PostgreSQL\15\bin`)

#### macOS:
```bash
brew install postgresql
brew services start postgresql
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### 2. Создание базы данных

Откройте терминал PostgreSQL:

#### Windows:
```bash
psql -U postgres
```

#### macOS/Linux:
```bash
sudo -u postgres psql
```

Затем выполните команды:

```sql
-- Создание базы данных
CREATE DATABASE url_shortener;

-- Подключение к базе (опционально, для проверки)
\c url_shortener

-- Выход из psql
\q
```

### 3. Импорт схемы базы данных

Импортируйте схему из файла `schema.sql`:

#### Windows (PowerShell/CMD):
```bash
psql -U postgres -d url_shortener -f schema.sql
```

#### macOS/Linux:
```bash
sudo -u postgres psql -d url_shortener -f schema.sql
```

Или через подключение psql:
```bash
psql -U postgres -d url_shortener
\i schema.sql
\q
```

### 4. Настройка окружения

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Отредактируйте `.env` файл:
```env
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@localhost:5432/url_shortener
PORT=3000
BASE_URL=http://localhost:3000
JWT_SECRET=ИЗМЕНИТЕ_НА_СЛУЧАЙНУЮ_СТРОКУ
```

**ВАЖНО:**
- Замените `ВАШ_ПАРОЛЬ` на пароль пользователя PostgreSQL
- Измените `JWT_SECRET` на случайную строку (минимум 32 символа)

### 5. Установка зависимостей

```bash
npm install
```

### 6. Запуск приложения

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Обычный режим
npm start
```

Приложение будет доступно по адресу: http://localhost:3000

## ✅ Проверка установки

1. Откройте браузер и перейдите на http://localhost:3000
2. Вы должны увидеть landing страницу
3. Попробуйте зарегистрироваться:
   - Нажмите "Sign Up"
   - Введите email и пароль (минимум 8 символов, буквы и цифры)
   - Проверьте консоль сервера для ссылки подтверждения email

## 🔍 Проверка подключения к базе данных

Если возникают проблемы с подключением:

```bash
# Проверка, что PostgreSQL работает
# Windows
pg_isready

# macOS/Linux
sudo -u postgres pg_isready

# Проверка подключения
psql -U postgres -d url_shortener -c "SELECT version();"
```

## 📧 Настройка Email (опционально)

По умолчанию используется тестовый SMTP (Ethereal Email). Ссылки для подтверждения email будут выводиться в консоль сервера.

Для production настройте реальный SMTP в `.env`:
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
```

## 🐛 Решение проблем

### Ошибка: "password authentication failed"
- Проверьте пароль в `.env` файле
- Убедитесь, что пользователь `postgres` имеет пароль

### Ошибка: "database does not exist"
- Убедитесь, что вы создали базу данных: `CREATE DATABASE url_shortener;`

### Ошибка: "relation does not exist"
- Убедитесь, что вы импортировали схему: `psql -U postgres -d url_shortener -f schema.sql`

### Ошибка подключения к PostgreSQL
- Проверьте, что PostgreSQL запущен:
  ```bash
  # Windows
  sc query postgresql-x64-15

  # macOS
  brew services list | grep postgres

  # Linux
  sudo systemctl status postgresql
  ```

### Порт 3000 занят
- Измените порт в `.env` файле: `PORT=3001`

## 📚 Структура базы данных

### Таблица `users`
- `id` - уникальный идентификатор
- `email` - email пользователя (уникальный)
- `password_hash` - хеш пароля
- `email_verified` - подтвержден ли email
- `verification_token` - токен для подтверждения email
- `reset_token` - токен для сброса пароля

### Таблица `urls`
- `id` - уникальный идентификатор
- `original_url` - оригинальный URL
- `short_code` - короткий код (6 символов)
- `user_id` - владелец ссылки
- `clicks` - количество переходов

### Таблица `clicks`
- `id` - уникальный идентификатор
- `url_id` - ссылка на URL
- `ip_address` - IP адрес посетителя
- `device_type` - тип устройства (Desktop, Mobile, Tablet)
- `os` - операционная система
- `browser` - браузер
- `is_unique` - уникальный клик (по IP за 24 часа)

## 🎯 Готово!

Теперь вы можете:
- ✅ Регистрироваться и входить в систему
- ✅ Создавать короткие ссылки
- ✅ Просматривать статистику кликов
- ✅ Генерировать QR коды
- ✅ Управлять своими ссылками в Dashboard

## 📝 Дополнительные команды

### Просмотр логов PostgreSQL
```bash
# Windows
# Обычно в C:\Program Files\PostgreSQL\15\data\log

# macOS
tail -f /usr/local/var/log/postgres.log

# Linux
sudo tail -f /var/log/postgresql/postgresql-*.log
```

### Резервное копирование базы данных
```bash
pg_dump -U postgres url_shortener > backup.sql
```

### Восстановление базы данных
```bash
psql -U postgres -d url_shortener < backup.sql
```
