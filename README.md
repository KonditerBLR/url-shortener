# 🔗 CutTo - URL Shortener

Профессиональный сервис сокращения ссылок с аналитикой, QR-кодами и поддержкой 3 языков (RU, EN, DE).

## ✨ Возможности

- 🔗 Сокращение длинных ссылок
- 📊 Детальная аналитика кликов (устройства, браузеры, ОС, география)
- 📱 Автоматическая генерация QR-кодов
- 👤 Система авторизации с верификацией email
- 🌍 Мультиязычность (Русский, English, Deutsch)
- 📈 Dashboard с real-time статистикой
- 🔒 Безопасность (JWT, bcrypt, защита от SQL injection)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

Создайте PostgreSQL базу данных:

```sql
CREATE DATABASE url_shortener;
```

Примените схему:

```bash
psql -U your_username -d url_shortener -f schema.sql
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните своими данными:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
DATABASE_URL=postgresql://username:password@localhost:5432/url_shortener
JWT_SECRET=your-super-secret-jwt-key-change-this
BASE_URL=http://localhost:3000
PORT=3000
```

### 4. Запуск сервера

**Режим разработки (с автоперезагрузкой):**
```bash
npm run dev
```

**Обычный запуск:**
```bash
npm start
```

Откройте в браузере: http://localhost:3000

## 📁 Структура проекта

```
url-shortener/
├── server.js           # Главный сервер
├── routes.js           # API endpoints
├── auth.js             # Авторизация и аутентификация
├── db.js              # Подключение к БД
├── email.js           # Отправка email
├── utils.js           # Утилиты
├── schema.sql         # Схема базы данных
├── public/            # Фронтенд
│   ├── landing.html   # Главная страница
│   ├── dashboard.html # Dashboard пользователя
│   ├── js/
│   │   ├── app.js         # Логика лендинга
│   │   ├── dashboard.js   # Логика dashboard
│   │   └── lang.js        # Система переводов
│   └── css/           # Стили
└── package.json
```

## 🔧 API Endpoints

### Публичные:
- `POST /api/shorten` - Создать короткую ссылку
- `GET /:shortCode` - Редирект по короткой ссылке
- `GET /api/qr/:shortCode` - Получить QR-код

### Авторизация:
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/forgot-password` - Восстановление пароля
- `POST /api/auth/reset-password` - Сброс пароля
- `GET /api/auth/verify-email` - Верификация email

### Защищенные (требуют токен):
- `GET /api/urls/user` - Получить свои ссылки
- `GET /api/urls/:id/stats` - Детальная статистика по ссылке
- `GET /api/stats/summary` - Общая статистика для dashboard
- `DELETE /api/urls/:id` - Удалить ссылку
- `GET /api/user/profile` - Профиль пользователя

## 🌍 Языки

Проект поддерживает 3 языка:
- 🇷🇺 Русский (ru)
- 🇬🇧 English (en)
- 🇩🇪 Deutsch (de)

Переводы находятся в `public/js/lang/*.json`

## 📊 База данных

### Таблицы:
- `users` - Пользователи
- `urls` - Короткие ссылки
- `clicks` - Клики и аналитика

Схема с индексами для оптимизации находится в `schema.sql`

## 🔒 Безопасность

- ✅ JWT токены для аутентификации
- ✅ Bcrypt для хеширования паролей
- ✅ Защита от SQL injection (параметризованные запросы)
- ✅ CORS настроен
- ✅ Верификация email
- ✅ Токены сброса пароля с истечением

## 🛠️ Технологии

**Backend:**
- Node.js + Express
- PostgreSQL
- JWT (jsonwebtoken)
- Bcrypt
- Nodemailer
- QRCode

**Frontend:**
- Vanilla JavaScript (ES6+)
- CSS3 с анимациями
- Fetch API
- LocalStorage для токенов

## 📝 Лицензия

MIT

## 🤝 Разработка

Создано с помощью Claude Code 🚀
